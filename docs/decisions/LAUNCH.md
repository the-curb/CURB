# Launch checklist — the credit desk on Robinhood Chain

**Status:** a checklist, not a decision. Prepared 13 September 2026. It executes [the token record](TOKEN.md) (decided by the product owner, 12 September 2026, Robinhood Chain included) by [the deployment plan](DEPLOYMENT.md) and [the operator policy](OPERATIONS.md), both still proposed. The treasury creation and signer record below are completed. No approved public CompanySeries deployment, configured CURB/CreditDesk deployment or independent review is established by this checklist. Every row says who does it, what it needs, the command, what the tool refuses when the row is not met, and what the site shows once it is. The tools are in `contracts/scripts/` and run from `contracts/`; every one reads Robinhood Chain (chain id 4663, the `robinhood-mainnet` profile) through `CURB_RPC_URL` when it is set in the shell and the public node `https://rpc.mainnet.chain.robinhood.com` otherwise — the deployment tool included, which says on its first line which node it is reading (the host only; a keyed URL is never printed and never written into a record). The tools read state — except the deployment tool's check of `priceSource.fromBlock` (row 12), which reads the pool's logs before that block: one query on the public node, which serves any width with fewer than 10,000 matches; on an endpoint that caps a query's width it walks down in pages, at most sixty-four, and says how far it read. Measured 13 September 2026: dRPC with a key (`lb.drpc.live`) serves full archive state and `eth_getLogs` up to 100,000 blocks with no cap on results, and is what the site's tick reads through (`CURB_RPC_URL` on Railway); dRPC without a key serves about 200 blocks a query and Alchemy's free tier ten — enough for the tools, not for the tick. The site's readers halve a query the node refuses for width, for matching too much, or for taking too long.

## Who

- **The product owner** decides: the signers, the launch, the pool. Decisions are written into the records by name and date; the site never upgrades one on its own.
- **The signers** — three people, each with a wallet of their own on Robinhood Chain — are the owners of the operator's Safe, 2-of-3 as the policy proposes. The Safe is the treasury every top-up goes to.
- **The reviewer** — a named person who is not the author of the record — reads what the tool wrote against the chain and signs the record with their name and the date. Without that name the deployment tool refuses.
- **The operator** holds the shell the tools run in, the deployer key (funded with ETH on Robinhood Chain for gas, never in this repository, never in a chat) and the Railway app service's variables.

One person may hold more than one of these; the records still say which.

## Where it stands

Done, and visible: the services and the gate (`/services`, `/api/credits`, the paid endpoints), the indexer, the rate reader with its guard, the credit desk contract with its unit tests, the deployment tools, the Safe tools, the CI rehearsal on a local chain, two adversarial reviews of the site code with the confirmed findings closed (commits `54ab36e` and `fc3df1d`). The build the site verifies a deployed desk against is `contracts/evidence/CreditDesk.build.json`, from the source at commit `06623ed`.

Done since (13 September 2026): rows 1 and 2 — three owners named by address and the Safe created at `0x4E69723F9Ba9fA2C9842d77b240b2C0F601ac219` (`contracts/evidence/safes/safe.4663.json`; owners and threshold read back from the chain, through the operator's endpoint and the public node); the deployer wallet exists and is funded. Not done: rows 3 onward. The site says so — `NOT_CONFIGURED` on `/services` and `/api/credits`, *not done* in the order of work — until they are.

**The public stage line (13 September 2026).** The product owner asked, the day the treasury went live, that the site’s stage read *mainnet*. It does: the front page’s kicker says *Stage · mainnet · Robinhood Chain*, and the line beneath it is derived from the record by `lib/launch/status.ts` — the Safe’s evidence file, the `CURB_CREDITS` line, the last code and rate reads, the credited top-ups — so it says exactly what is on chain (the treasury) and what is not (the token, the desk, the position series), and moves on its own as the rows below are done: a valid treasury creation record; a configured desk; matching code for the configured token and treasury; a rate read; a credited top-up tied to the current chain and desk index. Code and rate reads older than thirty minutes do not advance the displayed step; that threshold is a display rule, not an uptime guarantee. Missing evidence means unconfirmed, not proof that nothing exists on chain. The footer’s line (`BRAND.stage`) and the series’ stage line say the same in prose. None of the eight refused claims is made by any of it, and nothing is said to be sold.

## The rows

**Preparation prerequisites and execution order.** Before row 3, complete the independent-review scope required by TOKEN, its actual funding, launchpad/terms/authority review and the approved proceeds formula in the [mainnet dossier](../mainnet/PREPARATION.md). Rows 1–2 below preserve completed historical treasury actions; do not create another Safe simply to repeat them. Execute rows 3–10 after their respective decisions, then **12 → 13 → 11 → 14 → 15**: a supported, configured and freshly read pool precedes the first public-chain payment test. Unpriced-payment recovery belongs to an isolated rehearsal. The row identifiers remain stable for references; they are not an instruction to bypass these dependencies.

| № | Who | What | Needs first | Command or action | Refused when | Done shows as |
|---|---|---|---|---|---|---|
| 1 | Product owner, signers | Name the owners of the operator's Safe — at least three — and the threshold (2, as the policy proposes) | Three or more people with their own wallets on Robinhood Chain, none of them a signer for any issuer or custodian of a series' components; their addresses, written into [the operator policy](OPERATIONS.md) by name and date and published on the site (the register's A4) | A decision, not a command | `plan-safe.ts` refuses fewer than three owners, an owner listed twice, an address that is not 20 bytes of hex or fails its EIP-55 checksum, a threshold under 2 or over the count | The policy names them; the register's A4 closes. **Done 13 September 2026:** thecurb 1 `0xC33DAfAC1c65a92e956519Aa73E29a0b8c39214B`, thecurb 2 `0x5657dF9f072C536FcdE00BE2eF86c2240E4f8a53`, thecurb 3 `0x5cc762A11b05F9cCdaE3839C56F5Beb14C40F15D` |
| 2 | Operator | Create the Safe | Row 1; a funded wallet to send one transaction | `node scripts/plan-safe.ts <owner> <owner> <owner> --threshold 2` prints the `to` and `data` of `createProxyWithNonce` on Safe's factory and the predicted address, after simulating it on the node; then either any funded wallet sends those bytes, or the same command with `--send --reviewed` and `DEPLOYER_PRIVATE_KEY` in the shell sends it, waits for the receipt, reads the owners and the threshold back from the new Safe, compares them with what was asked and writes `evidence/safes/safe.4663.json`; the explorer is checked as well (`getOwners()`, `getThreshold()`) | The node does not answer, or answers another chain id; Safe's factory, singleton or fallback handler has no code; a contract already sits at the predicted address (that `--nonce` was used with these owners — choose another); the simulation disagrees with the prediction; with `--send`: no `--reviewed` off the local chain, no key in the shell, a sender with no ETH, an evidence file from an earlier creation, a receipt that did not succeed, a Safe that answers other owners or another threshold than asked | **Done 13 September 2026: `0x4E69723F9Ba9fA2C9842d77b240b2C0F601ac219`**, block 61,691,826, read back as asked. The Safe's address, with three owners and threshold 2 on the explorer; from here it acts through `safe-tx.ts --safe … --to … --data …` (the hash every owner approves, checked against the Safe's own; the `approveHash` bytes each owner sends from their wallet; and, with `--approved-by <owner>,<owner>` once two have approved on chain, the `execTransaction` bytes anyone sends) |
| 3 | Product owner | Launch the token | Funded and completed prerequisite review, actual launchpad/terms/allocation/vesting record, approved budget and explicit launch decision; reviewed exact-amount ERC-20 transfer behaviour and authority risks | Outside this repository, using the selected platform's reviewed mechanics. Record approval and authorized execution; no mechanics are assumed | A prerequisite without evidence is a hold; a name or UI flag is not a review | Actual-chain token address and deployment evidence for row 4, compared with the prelaunch record |
| 4 | Operator | Read the token into the record | Rows 2 and 3 | `node scripts/record-token.ts <token> --treasury <safe>` reads name, symbol, decimals, supply, code hash, the EIP-1967 proxy slots, `owner()` and `paused()` at a stated block and writes `records/credit-desk.4663.json` with those facts (`tokenAsRead`) beside the record, `rpcUrl` the public node, `priceSource` null and `reviewedBy` empty; without `--treasury` the treasury is written as the zero address, which the deployment tool refuses | The node does not answer, or answers another chain id; the token has no code; it does not answer `symbol()`, `decimals()` and `totalSupply()`; a function the node would not answer is a refusal, never a fact written down | The record file, with the facts and a note if the token sits behind a proxy (its code can change under its admin; the desk does not control it) |
| 5 | Reviewer | Review the actual record | Row 4 and the independently reviewed contract/build | Read `tokenAsRead` and source/authority facts against the actual chain; verify current treasury Safe identity, owners, quorum and controls against row 2's historical record; attach review evidence and only then fill `reviewedBy` and `reviewedAt` | Blank review fields are refused. A typed name alone cannot prove review; nonempty treasury code alone cannot prove a Safe | Reviewed deployment record linked to its source/build and report. This record check alone does not close A2's independent contract review |
| 6 | Operator | Dry run | Row 5 | `node scripts/deploy-credit-desk.ts records/credit-desk.4663.json --dry-run --reviewed` — on any chain but a local one `--reviewed` is needed on top of the record's own review, for the dry run too | In the tool's order: a misspelt flag (`--dryrun`), a `--flag=value`, an unknown flag — refused before the record is opened, never a real deployment; a record that cannot be read as JSON; no reviewer (row 5); a network that is not a profile the site knows, or a chain id that is not that profile's; a token or treasury address missing or zero, or the two the same; decimals outside 0–36; a malformed `priceSource`; on any chain but 31337, no `--reviewed`; then the node: it does not answer, or answers another chain id; the token has no code, does not answer `symbol()`, `decimals()` and `totalSupply()`, or answers other decimals than the record; the treasury has no code (the treasury is the multisig, not a key); the pool, if recorded, fails row 12's checks; the build record is missing, or the compiled contract is not the committed build record (rebuild with `npm run build`; `npm run check:build` says whether the record is this source's) | *dry run: every check passed; nothing was sent*, with the constructor arguments (the token, the treasury) printed |
| 7 | Operator | Deploy | Row 6; `DEPLOYER_PRIVATE_KEY` in the shell's environment — a key funded with ETH on Robinhood Chain, held by the operator, never in this repository and never in a chat | `node scripts/deploy-credit-desk.ts records/credit-desk.4663.json --reviewed` — the tool writes `evidence/deployments/credit-desk.4663.json` with the transaction hash and a *pending* note before it waits for the receipt (a cut-off leaves a note, not a second deployment on the next run), then rewrites it with the address, the block, the transaction and the constructor arguments, and prints the `CURB_CREDITS` line on stdout | Everything row 6 refuses; `evidence/deployments/credit-desk.4663.json` already exists (a desk was deployed from this record before — read it first); `DEPLOYER_PRIVATE_KEY` unset or not `0x` plus 64 hex digits, or holding no ETH on the chain (checked last, after every read, before anything is sent); the transaction did not succeed — the file then still holds the pending note with the transaction hash, and the next run is refused until a person has read it and moved it aside | The evidence file, committed; the `CURB_CREDITS` line for row 8 |
| 8 | Operator | Configure the site | Row 7; the store's schema current (`npm run db:migrate` from a shell with `CURB_POSTGRES_URL`, idempotent) | Set `CURB_CREDITS` on the Railway app service to the printed line — a bare JSON object; `priceSource` null until row 13 — (on Railway a variable change redeploys the service by itself; the change is live when that deployment is). `CURB_TICK_SECRET` must be set on the deployment (without it production `/api/tick` refuses requests with 503; local development may run without a secret) and the same value, with `CURB_TICK_URL`, in the repository's secrets for the scheduler. `CURB_RPC_URL` on the deployment stays the public node unless another is measured (see the header) | `/api/credits` answers `CONFIG_INVALID` with the reason: not JSON, not an object, an unknown network, a malformed token, desk or treasury address, the token and the desk sharing an address, the treasury equal to the token or the desk, a `fromBlock` that is not a non-negative integer, a `priceSource` that is not an object or null. A second desk in the same line (a redeployment) restarts the index from its own `fromBlock`; the earlier desk's credits stand under its address | `/api/credits` says `CONFIGURED`, the desk, the token and the treasury; the order of work on `/services` says *configured; code not yet verified* until the next tick |
| 9 | The tick | Verify the code | Row 8; the scheduler (GitHub Actions, every five minutes, with `CURB_TICK_SECRET`) | Nothing to do: the next tick reads the desk's code and compares it, outside the two immutable slots, with `contracts/evidence/CreditDesk.build.json`, and the slots with the record's token and treasury | A MISMATCH is a DARK condition on `/api/state` and, one tick later, on the webhook (a tick delivers alerts before it runs the desk); a MISMATCH, a code read the node did not answer (UNREAD) or a missing build record (NO_BUILD) all hold the index — nothing is indexed or credited from the desk — and `/services` composes no top-up bytes and says *do not send a top-up on the strength of this page* | *MATCHES the build at commit 06623ed…; the immutables hold the token and the treasury above* on `/services`; step 3 of the order of work is *done* |
| 10 | Operator | Verify the source on the explorer | Row 7 | Verify `contracts/src/CreditDesk.sol` at commit `06623ed` (the build record's `sourceCommit`; unchanged since) on `https://robinhoodchain.blockscout.com` with solc 0.8.30, EVM version prague, viaIR, optimizer on with 200 runs, `bytecodeHash: none` (the settings in `contracts/hardhat.config.ts`) and the printed constructor arguments — a third place to look for a reader who trusts neither this site nor the repository | — | The explorer shows the verified source at the desk's address |
| 11 | Operator | First small payment and service proof | Rows 9, 10, 12 and 13; current code/config/rate readiness; separate transaction authorization and test budget | Create and privately retain a key; use an operator-owned funded wallet other than the treasury. Review exact amount, quote age/buffer limits, approval and top-up bytes on `/services`. After the authorized transaction, reconcile receipt, treasury balance delta, index confirmations and credited dollars; complete a paid HTTP call and successful webhook using that key | `DeltaWrong`, transfer/allowance failures, absent or stale readiness hold the experiment. A missing index row is investigated, not treated as a zero balance. One transfer proves its observed path/amount, not every future token behaviour | Actual transaction and confirmed credited row tied to the current chain/desk, plus HTTP/debit/webhook evidence. Record any UNKNOWN settlement and reconcile it without blind retry |
| 12 | Product owner, operator | The pool | The token trades in a pool the reader knows on Robinhood Chain: a constant-product pair (`uniswap-v2-pair`: `getReserves()`, `Sync`) or a concentrated-liquidity pool (`uniswap-v3-pool`: `slot0()`, `liquidity()`, `Swap`). Its address and the block it was created in, read from the chain (the factory's `PairCreated` / `PoolCreated` event, or the pool's first transaction on the explorer) | Write `priceSource` into the record: `{ "kind": "uniswap-v2-pair", "pair": "0x…", "fromBlock": <creation block>, "quote": { "kind": "usd-stable" } }` for a CURB / USDG pool (USDG, six decimals, `0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168`, taken as dollars), or `"quote": { "kind": "chainlink-feed", "feed": "0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9" }` (ETH / USD) for a CURB / WETH pool; the reviewer signs the record again; then `node scripts/deploy-credit-desk.ts records/credit-desk.4663.json --dry-run --reviewed` checks the pool without sending anything (it notes that the evidence file already exists and that a real run would be refused — that is the point), `fromBlock` included: the tool reads the pool's logs before `fromBlock` and refuses when there is one (on a width-capped endpoint it reads down in pages and says how far); the site makes the same check once per pool on its first tick with the pool and holds the index while it disagrees. Two things the dry run cannot check are the reviewer's: that `kind` is the pool's real interface (a v2 pair and a v3 pool both answer `token0()`; the wrong kind is caught only at the tick — a pool whose price moved without a Swap the reader recognises is refused as UNREAD, never priced from its first price) and that a `usd-stable` quote's other side really is USDG (the reader takes whatever it is, at its own decimals, as dollars) | The pool has no code; it does not answer `token0()` and `token1()`; it holds neither side as the token; a feed does not answer `decimals()` and `latestRoundData()`; `fromBlock` is not a non-negative integer, is past the head, or is later than a log the pool emitted | The dry run passes with *pool: … holds the token against …* |
| 13 | Operator | Configure the pool | Row 12 | Set `CURB_CREDITS` with the reviewed `priceSource` and deploy that configuration. Obtain a fresh successful tick; rate/config/code identities must agree before row 11 | Invalid configuration, absent/currently unread or stale code/rate, wrong identity, insufficient price history or liquidity holds payment readiness | `/api/credits` and `/services` show the actual dated quote, source and buffer limitations; a rate alone is not a successful payment. If older top-ups already exist, reconcile them with their explicitly recorded pricing basis |
| 14 | Product owner | Decide public invitations and paid beta | Row 11's end-to-end result, current readiness, reviewed terms, assigned operations, budget/capacity and recorded scoped decisions | Review the mainnet dossier and readiness evidence; when authorized, publish the actual service scope and limits. Current milestones are derived from observations; a token or paid beta does not pass position gates | Missing required evidence or owner signoff remains HELD | Published invitation/beta decision tied to release and evidence; observed service milestones shown separately from the position prototype |
| 15 | Operator | Operate and verify recovery | Assigned primary/backup responders, monitoring, backup schedule and actual operating evidence | Tick indexes, verifies, prices and delivers on its configured schedule; monitor missed runs outside GitHub's failure domain, durable logs, pending payments and delivery backlog. Follow [RUNBOOK](RUNBOOK.md); take an off-provider backup on the agreed routine and before migration, and prove isolated restore. Record incidents and actual response/recovery times | Missing or unread monitoring is not a healthy status. CreditDesk has no pause: disabling invitations does not stop direct transfers | Dated health/backup/restore/incident evidence and measured beta outcomes, with known gaps visible |

## What is refused by design, and stays refused

- A record nobody reviewed by name; a public chain without `--reviewed`; a key from anywhere but the operator's shell.
- An address copied from a documentation example: every address in a record is read from the chain by a tool or from the explorer by the reviewer, and the reviewer's name says so.
- A second deployment over an existing evidence file; the file is moved aside on purpose, by a person, or it is not.
- An edit to `contracts/src/CreditDesk.sol` or to the compiler settings after the deployment: the site verifies the desk against the committed build record, CI refuses the edit until the record is re-recorded (`check:build`: the record is not this source's bytecode), and re-recording it turns a correct desk into a MISMATCH on `/services` and `/api/state`. A change is a new desk, a new record and a new deployment.
- A price typed in: the rate is read from a pool at a block or it is not stated. Until row 13 the price list is in dollars and nothing is quoted; a top-up meanwhile is indexed and waits.
- A promise the code does not keep: the eight claims the mechanism refuses for the position are refused for the token, and the services page says what the desk does, not what a launch might.

## What this checklist does not do

It decides nothing. The signers (A4), the reviewer (A2), the launch and the pool are people's decisions, recorded when made. It does not launch the token or choose where; it does not cover the position product's own deployment (rows 1–6 of [the deployment plan](DEPLOYMENT.md)), which waits on a gate decision of its own. It does not hold, make or ask for a key.
