{
  "name": "the-curb-contracts",
  "private": true,
  "version": "0.0.1",
  "description": "THE CURB — the series contract prototype and its tests. A design under test, unaudited, undeployed.",
  "type": "module",
  "scripts": {
    "build": "hardhat build",
    "test": "hardhat test solidity --grep-exclude Fork",
    "test:fork": "hardhat test solidity --grep Fork",
    "record:tests": "node scripts/record-tests.mjs"
  },
  "devDependencies": {
    "forge-std": "github:foundry-rs/forge-std#v1.16.2",
    "hardhat": "^3.16.0",
    "viem": "^2.56.3"
  }
}
